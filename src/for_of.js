let brausers = ['Chrome', 'Firefox', 'Opera', 'Safary', 'Edge'];

// получаем ключ
for (let brauser in brausers) {
   console.log(brauser);
}

// получаем значения
for (let br of brausers) {
    console.log(br);
}